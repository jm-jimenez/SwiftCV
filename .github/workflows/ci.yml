name: Simulator Tests

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - "**"
jobs:
  build-and-test:

    runs-on: self-hosted

    steps:
    - name: Checkout
    - uses: actions/checkout@v4
    - name: Set Default Scheme
      run: echo hiii
